# Point the frontend at your local ERAP API.
# Use the HTTPS URL if you've trusted the dev cert (dotnet dev-certs https --trust),
# or the HTTP URL to skip cert trust entirely.
# VITE_API_BASE_URL=https://localhost:7188
VITE_API_BASE_URL=http://localhost:5188
